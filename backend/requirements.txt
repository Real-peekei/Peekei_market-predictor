pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3,<2.0
yfinance>=0.2.40
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.29
joblib>=1.3

# Note on scikit-learn versions: trained model files (.pkl in backend/models/)
# are tied to the scikit-learn version active when they were trained. If you
# upgrade/reinstall scikit-learn later, OLD .pkl files can fail to load with
# errors like "ModuleNotFoundError: No module named '_loss'". Fix: retrain
# (python train_all.py) so all models are regenerated under your current
# scikit-learn version. Check your version with: pip show scikit-learn
